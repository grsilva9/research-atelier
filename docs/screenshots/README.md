# Screenshots

Add PNG screenshots here after running the app once. Suggested captures:

- `board.png` — the Kanban board view with a few papers
- `focus.png` — the Focus view with the "one thing today"
- `detail.png` — a paper detail panel with sections expanded
- `log.png` — the diary view with a weekly review

These are referenced from the main README. Keep them around 1600×1000 for retina quality, compressed with something like `pngquant` to keep the repo small.
